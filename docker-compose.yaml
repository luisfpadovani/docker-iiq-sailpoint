version: '2.3'
services: 
    db:
        container_name: bd_sql_iiq
        build: 
            context: ./sqlserver
        restart: always
        ports: 
            - 1433:1433
        command: 
            bash -c "sh -x /tmp/entrypoint.sh"
